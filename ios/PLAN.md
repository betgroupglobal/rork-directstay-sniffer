# Rebuild app to show latest version in preview

The app code is already up-to-date with all the latest features:

- **Search** with location autocomplete, dates, guests, filters
- **Airbnb + Crawl** combined search results
- **Direct Booking Hunter** for Airbnb listings
- **Explore** map view with property pins and bottom sheet
- **Favorites**, **Alerts**, and **Settings** tabs

**What needs to happen:** Simply rebuild and redeploy the app so the preview shows the current code instead of the old first iteration.